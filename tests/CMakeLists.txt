INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/include)
#	FOREACH(_BUILD_TYPE ${D2K_BUILD_TYPES})
#		IF (${_BUILD_TYPE} MATCHES "DEBUG")
#			SET(TEST_LIBRARIES_${_BUILD_TYPE} ${D2K_LIBRARIES_DEBUG})
#		ELSE()
#			SET(TEST_LIBRARIES_${_BUILD_TYPE} ${D2K_LIBRARIES_RELEASE})
#		ENDIF()
#	ENDFOREACH()
DEAL_II_PICKUP_TESTS()

#MESSAGE("--------------------------------------${_lib}")
INCLUDE_DIRECTORIES(${SUNDIALS_DIR}/include)
INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/include)
